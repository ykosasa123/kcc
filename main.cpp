#include <iostream>
#include <string>

#include "lex.hpp"
#include "Token.hpp"

int main(int argc, char *argv[])
{
    if (argc < 2) {
        std::cerr << "prog paramater not found." << std::endl;
        exit(-1);
    }

    return 0;
}
